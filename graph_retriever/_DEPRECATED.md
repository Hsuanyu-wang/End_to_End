# DEPRECATED

此 top-level `graph_retriever/` 目錄已被 `src/graph_retriever/` 取代。
僅 `legacy/` 下的舊模組仍引用此目錄。

新的 graph retrieval 實作請使用：
- `src/graph_retriever/lightrag_graph_retriever.py` (Entity Linking + Strategy)
- `src/graph_retriever/strategies/` (one_hop, ppr, pcst, tog)
