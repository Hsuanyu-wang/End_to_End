[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    -ra
asyncio_mode = auto

markers =
    unit: 單元測試
    integration: 整合測試
    slow: 慢速測試
    asyncio: asyncio 測試（pytest-asyncio）
